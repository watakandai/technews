# Virtual environment
.venv/
venv/

# Python
__pycache__/
*.pyc
.pytest_cache/

# Runtime data - generated by `cli fetch`, not source code. The
# docs/data/items.json export IS meant to be committed (that is what GitHub
# Pages serves); only the raw database is excluded.
*.db
*.db-journal

# OS cruft
.DS_Store
